#include "main.h"
/**
 *add - adds two integers and returns the result
 *@y:computed number
 *@z:computed number
 *Return: sum of y and z
 */
int add(int y, int z)
{
	int sum = y + z;

	return (sum);
}
